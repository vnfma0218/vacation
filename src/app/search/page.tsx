import SearchBody from '@/components/SearchMain';

const page = ({}) => {
  return (
    <main className="max-w-6xl mx-auto px-10 pt-10">
      <SearchBody />
    </main>
  );
};

export default page;
